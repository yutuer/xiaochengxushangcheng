<!--components/numOpera/index.wxml-->
<view class="numOpera">
    <image hidden="{{!subImgShow}}" src="{{subImg}}" catch:tap="subClick"
           style="height:{{imgHeight}}rpx;width:{{imgWidth}}rpx;"/>
    <text hidden="{{!numShow}}" style="width:{{textWidth}}rpx; line-height:{{imgHeight}}rpx">{{showNum}}</text>
    <image src="{{plusImg}}" style="height:{{imgHeight}}rpx;width:{{imgWidth}}rpx;" catch:tap="addClick"
           hidden="{{!plusImgShow}}"/>
</view>